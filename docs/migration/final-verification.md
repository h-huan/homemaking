# SaaS 迁移验收记录

日期：2026-09-02。工作分支：`saas-platform`。实施过程中未修改 main、原 D:\code\homemaking 工作区或同步项目 sources。

## 基线与成果

后端：YunaiV/ruoyi-vue-pro `ae176c856ac34291ecd3d3c89f0711eed39f9b49`。前端：yudaocode/yudao-ui-admin-vue3 `e272d6639bcf87e0259bcb6ede721da4d047b2bc`。使用 Java 17 模块化单体、Vue 3 + TypeScript + Element Plus；来源和许可证保留在 LICENSE、THIRD-PARTY-NOTICES.md 等文件。

`com.hm`、hm-*、Hm* 及启用配置已统一，system_/infra_/pay_/mp_ 表前缀保持不变。默认仅启用 system、infra、pay、mp、homemaking；可选模块源码保留。旧应用完整保存在 legacy，便于对照和追溯。

已接入的平台能力：Customer 与 tenant relation、微信统一身份、总部直营/加盟模式、品牌与域名、平台/租户/客户三级通知约束。业务能力包括门店/人员/服务管理、服务器计价、SKU/加项/区域价格、预约占位、订单流程、支付/退款适配、评价、售后和待结算账目。小程序已有 API 迁接，支持支付发起、提醒偏好和用户主动订阅入口。

## 已完成验证

| 项目 | 结果与范围 |
| --- | --- |
| 后端构建 | 24 个启用 reactor 模块 Maven package 通过；Java 17 |
| 选定回归 | 34 项通过：业务与租户隔离 15、微信身份 5、通知规则 9、支付权限 4、脱敏 1 |
| 前端 | 锁文件依赖安装通过；完整 vue-tsc 检查通过；生产 Vite 构建通过 |
| 小程序 | 24 个 JavaScript 文件语法检查通过；尚未在微信开发者工具或真机完整验收 |
| 服务启动 | 独立 Redis + H2 MySQL 兼容模式启动通过；未连接已有业务库 |
| HTTP | 正常 admin 登录、门店新增/列表、公开品牌和首页成功；缺租户 400、跨租户 403、匿名上传 401、后台冒充客户 403 |
| 页面 | 登录、首页统计、门店列表、白标保存回显、通知策略页面检查通过；首批模块导航正确；刷新后主色 #136f63 正确生效 |
| 迁移映射 | 合成旧库数据迁入：12345 分订单金额、1000 分已退款金额保留；原分类编号 9 保留；人员档案编号 7 与登录账号编号 2 正确区分；按历史预约时间保留 4 个半小时时段；17 行来源归档；重复导入被拒绝 |
| 安全检查 | 上传、匿名接口、租户隔离、支付身份/回调、凭据与日志；测试令牌请求值未进入最终运行日志；详见 security-review.md |

测试命令和可复现迁移映射检查在 runbook.md 与 tools/migration/README.md 中。34 项属于选定回归，**不是全仓所有上游测试**。曾发现并修正 HM 化导致的脱敏测试夹具不一致，以及 Windows SQL 夹具中文编码问题。

## 尚未完成的上线条件与功能边界

- 本次没有生产数据库快照、微信应用密钥、支付商户配置，因此没有执行生产导入、真实登录联调、消息送达、扣款、退款或正式部署。
- 原生 MySQL 8.0.16+ 迁移尚未演练。H2 检查包含方言适配，不能证明全部 MySQL DDL/SQL 的原生执行行为。下载独立原生数据库运行环境失败，现有 MySQL 5.7 不满足 CHECK 约束要求。
- 旧人员服务范围/区域关系、独立排班、线索等保存在来源归档中，尚未全部接入新管理页及规则；正式业务切换前需补齐。当前新预约使用门店归属、服务区域、预约规则和订单占位校验。
- 当前 Settlement 是完工订单的待结算账目，不包含自动打款、分账和财务结账。历史支付/退款需人工对账后才能接入新支付系统。
- SKU、加项、服务区域、预约规则数据已迁入并参与计价/预约；本次运营页面优先提供门店、人员、基础服务和订单操作，尚未提供全部高级配置的编辑器。
- 未启用模块保留上游来源及历史示例，不代表已通过安全验收或可以直接开放。启用时需另行检查依赖、权限、配置与业务约束。

因此，本分支可用于代码审查、开发验证和下一步原生迁移演练；尚不应直接切换生产流量。
