# 凭据来源与清理记录

日期：2026-09-02。范围：本地 `saas-platform` 副本的当前文件和本次未发布的迁移提交。

## 为什么触发推送保护

截图中的腾讯云、阿里云、火山引擎标识位于原迁移提交 `0726bc7` 的 `sql/highgo/ruoyi-vue-pro.sql` 第 468–470 行，是 `infra_file_config` 中的云存储示例。逐项比对确认：accessKey 和配套 accessSecret 均与指定后端上游 `ae176c856ac34291ecd3d3c89f0711eed39f9b49` 完全相同。本地来源副本为该 commit；真实上游地址见 [THIRD-PARTY-NOTICES](../../THIRD-PARTY-NOTICES.md)。

这些值随上游导入，并非用户为新平台提供的配置。此前仅清理启用配置和新建库 SQL，遗漏保留的其他数据库方言、接口/测试示例及提交历史，是迁移检查遗漏。上游公开并不能证明凭据已经失效；没有调用云服务验证，也没有使用它们。

## 实际整改

- 清理 8 种数据库方言中的云存储 accessKey/accessSecret 等配置值，保留表结构、业务前缀和来源信息；当前部署仍只使用部署说明列出的五份 HM MySQL SQL。
- 清理 AI、短信、IoT、HTTP 调试等示例中的硬编码凭据/令牌；保留模块源码，需另行填入自己的私有配置才能运行真实集成测试。
- 移除当前及 legacy 前端内置 RSA 私钥；兼容工具不再保存密码。当前登录缓存只保留账号/租户等信息，读旧缓存时清空密码，保存时不修改正在提交的登录表单。
- 加密示例改用每次运行生成的临时密钥，验证加解密往返，不打印密钥。
- 清理 legacy 配置中的密码、微信密钥等字面量。legacy 是迁移参考，不应按占位配置直接启动。
- 将上述清理应用到四个未发布的阶段提交中，每个提交清理 96 个文件；不是只在末尾追加“删除密钥”的提交。保留各阶段提交消息、作者和时间。
- 添加 [提交前检查](../../tools/security/README.md)：全默认 Gitleaks 规则加腾讯云/火山引擎标识规则；检查当前文件和待推送提交，不使用密钥忽略白名单或推送保护 Bypass。

阶段编号已变化，不应再把旧四个提交合并或 cherry-pick 回来：

| 阶段 | 原提交 | 清理后提交 |
| --- | --- | --- |
| HM 底座 | `0726bc7` | `5358b6d` |
| 家政业务 | `d3c29a5` | `41e33e8` |
| 安全与验收 | `c6100d5` | `1b482ae` |
| 品牌主题 | `5315eb0` | `7df504e` |

## 验证结果

使用 Gitleaks 8.30.1 官方 Windows x64 发布包，SHA-256 与该次发布的 checksums 文件一致；扫描输出脱敏。

| 检查 | 结果 |
| --- | --- |
| 原四个迁移提交，默认规则 | 152 处告警，包含重复值、示例和测试夹具，不等同于 152 个有效密钥 |
| 当前全部受管理文件及未忽略的新文件，默认规则 + 云标识规则 | 0 处告警 |
| 重写后的四个迁移提交，同上规则 | 0 处告警 |
| 重建的前端 `dist-prod` | 0 处告警 |
| 前端生产构建 / 完整类型检查 | 通过 |
| 登录缓存行为 | 读旧缓存删除密码；写缓存排除密码；不改变登录请求中的密码 |
| 加密回归 | 3 项通过；无硬编码密钥或密钥日志 |
| 生产模式模板加载 | 正确覆盖本地开发地址；同域 API、dist-prod、空凭据、租户/验证码开关均符合说明 |

扫描器不能保证发现所有形式的凭据；不能把 0 告警当作密钥撤销或完整安全认证。

## 迁移之前已有的历史问题

原根提交 `7156dcb` 的默认扫描仍有 **7 处疑似凭据告警**，涉及旧 `hm-admin` 的数据库/Redis/微信配置和旧前端私钥。它已经是远端 `main` 与 `saas-platform` 的共同根提交，早于本次迁移；这些来源应由原项目账号持有人核对，不能都归因于本次芋道上游。

本次已经清理新分支中的 legacy 文件，但按“不修改 main”的要求，**没有重写这个共享根提交**。因此完整分支历史仍会报告该根提交的遗留值。若其中数据库密码、Redis 密码、微信密钥曾实际使用，应在所属环境轮换/撤销；删除 Git 文本不能使原凭据失效。共享根历史的清理需要单独处理仓库各分支及协作者，不属于这四个未发布迁移提交的改写。

远端只读核对时，`main` 与 `saas-platform` 均仍为 `7156dcb`。此次没有推送、强制推送、绕过扫描或生产部署。旧本地提交对象可能仍可通过 Git reflog 查到，但已不在新的迁移提交链上；没有创建含旧迁移提交的备份分支。

实际部署应从 [deploy 配置模板](../deployment/README.md) 填写自己的值，具体替换位置见[环境变量清单](../deployment/environment-variables.md)。
