# 上游脚本参考目录

本目录保留上游脚本与历史示例。`shell/deploy.sh` 的 profile、服务器路径，以及 `docker/docker-compose.yml` 的数据库、前端目录和环境变量尚未适配本次 SaaS 版本，不能直接用于当前部署。

请使用[当前部署步骤](../docs/deployment/README.md)、[变量替换清单](../docs/deployment/environment-variables.md)及 `deploy/*.example`。不要使用示例中的默认账号、开发 Token 或旧 SQL 初始化生产库。
